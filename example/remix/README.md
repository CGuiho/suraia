# Suraia Remix SQL Generation Example

Status: planned.

This directory is reserved for a future Remix / React Router framework-mode target with SQL generation.

## Planned Direction

- Generate route modules, loaders, actions, and local components from Suraia blueprints.
- Map form blueprints to typed server actions.
- Generate SQL scaffolds for data-backed pages where requested.
- Keep the UI blueprint dependency resolution contract intact before generating route-level code.
