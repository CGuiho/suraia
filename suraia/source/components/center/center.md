# Center

Centers content vertically and horizontally using flexbox.

## AI Translation Notes
- Default element is `div`.
- Uses `display: flex` with `align-items: center` and `justify-content: center`.
- When `inline` is set, uses `display: inline-flex`.
