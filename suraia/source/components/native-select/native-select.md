# NativeSelect

Native select wrapper element.

## AI Translation Notes
- Renders as a wrapper containing a `<select>` tag.
- Reuses browser native select panel for mobile and touch devices.
- Uses `appearance: none` to override styling and custom CSS arrows.
