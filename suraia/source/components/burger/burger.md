# Burger

A hamburger menu toggle that animates between three bars and an X shape.

## AI Translation Notes
- Three `<span>` bars inside a container.
- On `[data-suraia-opened]`, top bar rotates +45°, middle fades, bottom rotates -45° to form an X.
- Always requires `aria-expanded` and `aria-label="Toggle navigation"`.
