# ThemeIcon

A styled container for icons, supporting visual variants, sizes, colors, and border-radius.

## AI Translation Notes
- Renders as a `<span>` visual icon wrapper.
- Use `color-mix` with `var(--suraia-alpha-10)` for the translucent tinted background of the `light` variant.
- Center aligns SVGs or icon fonts using flexbox.
