# Card

Card component with options for padding, shadows, border radius, and full-width card sections.

## AI Translation Notes
- CardSections inside Card negate Card padding using negative margins `calc(-1 * padding)`.
- Support standard spacing scale for padding (`xs` to `xl`).
- Default backdrop color uses theme's paper/card background.
