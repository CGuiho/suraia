# Blockquote

A styled quote block, with optional icon, author citation, and colored left border indicator.

## AI Translation Notes
- Renders as a `<blockquote>` HTML element.
- Provides slots for the main quote, `cite`, and an optional prefix `icon`.
- Features a left border highlight styled dynamically with the `color` attribute.
- Uses `var(--suraia-surface-foreground-muted)` for the citation text color.
