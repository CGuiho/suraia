# UnstyledButton

A plain button element that resets default browser button styles.

## AI Translation Notes
- Renders as a `<button type="button">` or custom polymorphic tag (e.g. `a`).
- Clears native borders, backgrounds, outlines, padding, and fonts.
- Map `disabled` to HTML `disabled` attribute or `aria-disabled="true"`.
