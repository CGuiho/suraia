# Container

A centered content wrapper with max-width constraint. Fluid mode removes the max-width.

## AI Translation Notes
- Simple `margin: 0 auto` + `max-width` pattern.
- Padding provides horizontal gutters.
- `fluid` removes max-width for full-width sections.
