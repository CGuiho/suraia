# Fieldset

Input grouping element with legend title.

## AI Translation Notes
- Renders as a native `<fieldset>` tag with optional `<legend>`.
- Setting `disabled` on the native `<fieldset>` element automatically disables all interactive children inputs natively.
