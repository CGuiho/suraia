# Breadcrumbs

Navigation showing the current page hierarchy with configurable separator.

## AI Translation Notes
- Wrap in `<nav aria-label="Breadcrumb">`.
- Last item uses `aria-current="page"` and is not a link.
- Separators are `aria-hidden="true"`.
