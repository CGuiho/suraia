# PasswordInput
A text input with a toggle button to show/hide the password.

## AI Translation Notes
- Uses `type="password"` by default, toggled to `type="text"` on visibility toggle.
- Toggle button has `tabindex="-1"` to prevent tab-stop interference.
- Inherits all TextInput styling.
