# FileInput

Upload field containing filenames list.

## AI Translation Notes
- Renders as a wrapper box with a hidden `<input type="file">`.
- Trigger is a styled placeholder field.
- Clicking the styled field should trigger input.click().
- Listen to input `change` events and map values back to controller `handleFilesChange()`.
