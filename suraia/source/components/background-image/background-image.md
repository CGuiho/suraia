# BackgroundImage

A container that displays an image as its background.

## AI Translation Notes
- Renders as a `<div>` or wrapper element.
- The image source is passed directly into the inline `background-image` style property.
- Support standard `radius` keys like `xs`, `sm`, `md`, `lg`, `xl`, `full`.
