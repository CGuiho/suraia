# CloseButton

A small X icon button for dismissing modals, alerts, notifications, etc.

## AI Translation Notes
- Renders an inline SVG X icon inside a `<button>`.
- Always requires `aria-label="Close"`.
- Subtle variant shows hover background; transparent only changes icon color.
