# Flex
A CSS flexbox layout component. All properties are set via CSS custom properties for maximum flexibility.

## AI Translation Notes
- Pure CSS layout utility — no JavaScript behavior needed.
- All flex properties (direction, wrap, justify, align, gap) set via `--suraia-flex-*` custom properties.
