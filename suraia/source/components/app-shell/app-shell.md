# AppShell

Application frame shell.

## AI Translation Notes
- Responsive structure maps main content offset margins directly to `--suraia-shell-*` variables.
- Uses semantic HTML `header`, `nav`, `aside`, `main`, and `footer` tags.
- Handles fixed or scrollable sidebar behaviors.
