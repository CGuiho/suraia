# SegmentedControl

A horizontal (or vertical) set of exclusive toggle buttons. Visually similar to a pill-shaped switcher.

## AI Translation Notes
- Uses hidden radio inputs for semantics; visual styling via labels.
- Active item gets a white/surface background with shadow.
- The container has a muted surface background, creating the "pill" effect.
- Arrow keys navigate between options (left/right for horizontal, up/down for vertical).
