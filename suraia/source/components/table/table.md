# Table

A styled HTML table with optional striped rows, hover highlighting, sticky header, and border options.

## AI Translation Notes
- Uses native `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>`.
- Container div provides `overflow-x: auto` for horizontal scrolling.
- All display features are controlled via `data-suraia-*` attributes on the `<table>`.
- Header uses uppercase, small text, and semibold weight.
