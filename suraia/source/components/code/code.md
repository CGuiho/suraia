# Code

Inline or block code wrapper with monospace font styling.

## AI Translation Notes
- Renders as a `<code>` tag.
- Multi-line block code uses a `<pre>` element wrapper.
- Uses system/monospace font family stack.
- The `block` variant is styled with display block and inline scrollbar behavior.
