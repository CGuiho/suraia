# Iteration 1

- [ ] Design JSON Schema for tokens format
- [ ] Create `schemas/tokens.json` in the repository
- [ ] Update the `$schema` reference in `suraia/source/themes/tokens.json` to point to the raw GitHub URL
- [ ] Verify that IDE autocompletion/validation works against the new schema file
