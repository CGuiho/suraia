# Iteration 1

- [ ] Audit package.json files/include fields
- [ ] Audit jsr.json files/include fields
- [ ] Compare include/files configs with files actually needed in the distribution (e.g. docs, library, src, tsconfig, etc.)
- [ ] Test JSR / NPM packaging locally to ensure no missing files
